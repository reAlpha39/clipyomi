{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core event delivery for parse results.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen"]
}
